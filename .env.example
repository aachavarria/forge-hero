# ── Portrait generation (optional) ────────────────────────────────────────
# Without a key, image UPLOAD still works; only AI generation is disabled.
# Get a key at https://aistudio.google.com/apikey then copy this file to
# .env.local and restart `npm run dev`.
GEMINI_API_KEY=

# Optional: override the image model (default: gemini-2.5-flash-image)
GEMINI_IMAGE_MODEL=gemini-2.5-flash-image

# ── Card art (R2) ─────────────────────────────────────────────────────────
# Public base URL of the R2 bucket holding the scraped card art. Optional —
# the app defaults to the project's public r2.dev bucket. Override this to
# point at your own bucket or a custom CDN domain.
# NEXT_PUBLIC_ → inlined at build time, so set it before `next build`.
NEXT_PUBLIC_R2_BASE=
